apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-25
  namespace: namespace-1
spec:
  selector:
    matchLabels:
      app: nginx-25
  replicas: low-urgency
  template:
    metadata:
      labels:
        app: nginx-25
        team: cs
      annotations:
        env: staging
        on-call-priority: 
    spec:
      containers:
      - name: nginx-25
        image: nginx:1.14.2
        ports:
        - containerPort: 80
