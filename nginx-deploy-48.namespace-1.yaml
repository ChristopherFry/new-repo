apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-48
  namespace: namespace-1
spec:
  selector:
    matchLabels:
      app: nginx-48
  replicas: low-urgency
  template:
    metadata:
      labels:
        app: nginx-48
        team: cs
      annotations:
        env: staging
        on-call-priority: 
    spec:
      containers:
      - name: nginx-48
        image: nginx:1.14.2
        ports:
        - containerPort: 80
